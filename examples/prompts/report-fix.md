You are executing run $run_index of $run_count for one OC Mass Operations item.

Run:
$run_json

Item:
$item_json

Payload:
$payload_json

Fix issues identified by the review run for item $item_id only.

Keep the fix scoped to the current item and run the smallest relevant verification before finishing.
