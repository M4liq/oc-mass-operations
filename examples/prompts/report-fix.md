You are executing run $run_index of $run_count for one OC Mass Operations work unit.

Run:
$run_json

Item:
$work_unit_json

Payload:
$payload_json

Fix issues identified by the review run for work unit $work_unit_id only.

Keep the fix scoped to the current work unit and run the smallest relevant verification before finishing.
