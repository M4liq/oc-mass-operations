You are executing run $run_index of $run_count for one OC Mass Operations item.

Run:
$run_json

Item:
$item_json

Payload:
$payload_json

Implement the requested report-like artifact changes for item $item_id only.

Use the branch from the item payload when the workflow requires a branch. Do not perform review or fix-review work in this run; leave that for later runs.
