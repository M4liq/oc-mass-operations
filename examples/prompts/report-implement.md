You are executing run $run_index of $run_count for one OC Mass Operations work unit.

Run:
$run_json

Item:
$work_unit_json

Payload:
$payload_json

Implement the requested report-like artifact changes for work unit $work_unit_id only.

Use the branch from the work unit payload when the workflow requires a branch. Do not perform review or fix-review work in this run; leave that for later runs.
