You are executing run $run_index of $run_count for one OC Mass Operations work unit.

Run:
$run_json

Item:
$work_unit_json

Payload:
$payload_json

Review the current changes for work unit $work_unit_id only.

Report concrete issues, risks, and missing verification. Do not make code changes in this run unless the configured opencode agent requires a minimal metadata update.
